<div id="<%= parent_class %>" class="container-fluid">
    <div class="row no-gutters">
        <div class="panel panel--left col-md-12 col-lg-6 col-xs-12 col-sm-12">
            <div class="top--nav"></div>
        </div>
        <div class="panel panel--right col-md-12 col-lg-6 col-xs-12 col-sm-12">
            <div class="panel--content">
                <div class="header mb-5">
                    <h3 class="text-center">登録</h3>
                </div>
                <% unless signed_in? %>
                <div class="signup--form">
                    <div class="row justify-content-center mb-2">
                        <%= link_to "/auth/facebook", class:"btn btn-social btn-facebook" do %>
                            <span class="ti-facebook"></span>ファスプークを使う
                        <% end %>
                    </div>
                    <div class="row justify-content-center">
                        <%= link_to "/auth/twitter", class:"btn btn-social btn-twitter" do %>
                            <span class="ti-twitter"></span>トィッタを使う
                        <% end %>
                    </div>
                </div>
                <% else %>
                    <h1>Xin chao <%= @current_user&.name %>
                <% end %>
            </div>
        </div>
    </div>
<div>